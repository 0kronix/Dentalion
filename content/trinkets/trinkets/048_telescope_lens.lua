Dentalion_API.Trinket {
    key = "telescopelens",
    pos = Dentalion.get_atlas_pos(48, 10),

    config = { extra = {  } },

    atlas = 'trinkets',
    unlocked = true,
    loc_vars = function(self, info_queue, card)
        return {
            vars = {  }
        }
    end,
}